// Do not change or turn in this file.
#ifndef Q1_H
#define Q1_H

#include <string>
using namespace std;

// Returns true if characters in the string are ordered according to the ASCII character table (from lower to higher values), and false otherwise.
bool isOrdered(string s);

// Returns true if the digits in the number are in increasing order, and false otherwise.
bool isOrdered(unsigned int i);

#endif
